Session-start protocol + rules live in the sibling docs repo's `CLAUDE.md`. Read that first.

This file is the pm4ai-managed agent rules file for the code repo; on `bunx pm4ai@latest fix` it regenerates from rules in pm4ai. Code-repo-specific gotchas land in the docs repo's `GOTCHAS.md`, not here.
